{
  "name": "@plucker/native-context-menu",
  "version": "0.0.0",
  "private": true,
  "description": "Native SwiftUI context-menu panel for Plucker (macOS 13+)",
  "main": "index.js",
  "types": "index.d.ts",
  "os": [
    "darwin"
  ],
  "scripts": {
    "build": "node scripts/build.mjs",
    "build:debug": "node scripts/build.mjs --debug",
    "rebuild": "node scripts/build.mjs --clean",
    "clean": "rm -rf .build"
  },
  "dependencies": {
    "node-swift": "1.4.0"
  }
}
